import { FocusVisibleDirective } from './focus-visible.directive';

describe('FocusVisible', () => {
  it('should create an instance', () => {
    const directive = new FocusVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
